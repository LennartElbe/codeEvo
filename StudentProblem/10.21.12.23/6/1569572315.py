import functools
import typing
import string
import random
import pytest

def list_filter(x,[xs]):
    Liste = []
    if x or xs = None:
        return None
    elif x =! None:
        l = len([xs])
        for x in xs in range(l)
            if x > xs
                Liste.append(xs)
            
            
