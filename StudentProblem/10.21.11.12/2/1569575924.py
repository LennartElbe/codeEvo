import functools
import typing
import string
import random
import pytest

def is_palindromic(n: int) -> int:
    if n <= 0:
        print("n ist kein Palindrom")
    else:
        str_n = str(n)
        return
        
