import functools
import typing
import string
import random
import pytest

def list_filter(x: int, xs: list) -> list:
    filtered_list = []
    i = 1
    while i < len(xs)
        if x <= xs[i]:
            xs[i] += filtered_list
            i += 1
        elif x >= xs[i]:
            i += 1
    return filtered_list
