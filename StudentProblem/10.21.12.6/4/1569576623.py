import functools
import typing
import string
import random
import pytest

class Vigenere:
    def __ init__(self, key, text):
        self.key = key
        self.text = text
        
    if key == None:
        return
