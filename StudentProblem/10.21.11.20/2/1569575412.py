import functools
import typing
import string
import random
import pytest

def is_palindromic(n: int) -> Bool:
    n > 0
    for i in str(n) range(len(str(n)):
        p = reverse(str((n)) #vorwärts
            if i == p:
                return True
            else:
                return False
