import functools
import typing
import string
import random
import pytest

class Vigenere():
    """Class Vigenere
    """
    def __init__(self, key):
        self.key = key
        
    def encrypt(w: str):
        z = key * len(w)
        return ord(z)
        
            
    
    def decrypt(wcrypt: str):
        return chr(wcrypt)
        
        
        
    
