import functools
import typing
import string
import random
import pytest

class Vigenere:
    def __init__(self, keyword, encrypt, decrypt):
        self.key = key
        self.encrypt = encrypt
        self.decrypt = decrypt
    def self.encrypt(x : str):
        return x.upper
    def self.decrypt(y : str):
        return y.upper
