import functools
import typing
import string
import random
import pytest

def klein(x:int ,xs:list) -> list:
    result = []
    for i in xs:
        if x > xs:
            append.result(xs)
    return result
