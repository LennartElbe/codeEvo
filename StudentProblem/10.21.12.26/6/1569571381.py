import functools
import typing
import string
import random
import pytest

# Lösung Teil 1.
def klein(x:int ,xs:list) -> list:
    result = []
    for i in xs:
        if x > xs:
            append.result(xs)
    return result
######################################################################
# Lösung Teil 2. (Test)


######################################################################
