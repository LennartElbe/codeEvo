import functools
import typing
import string
import random
import pytest

def is_palindromic(n: int) -> float:
    n > 0
    n = []
    if n [:-n] = [+:]:
        print("n = Palindrom")
    else:
        print("n ist kein Palindrom")
    
