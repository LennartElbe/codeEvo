import functools
import typing
import string
import random
import pytest

def divisors(n: int, d: int) -> list:
    if d // n == 0:
        print("Teiler")
    else:
        print("Kein Teiler")
        

        
